name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.dev.txt

      - name: Run tests (skip model tests — models not in CI)
        run: pytest tests/ -v -m "not requires_model" --ignore=tests/test_model.py

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Build API image
        run: docker build -f Dockerfile.api -t threat-api:${{ github.sha }} .

      - name: Build dashboard image
        run: docker build -f Dockerfile.dashboard -t threat-dashboard:${{ github.sha }} .